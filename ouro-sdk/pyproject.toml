[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ouro-sdk"
version = "0.1.0"
description = "Python SDK for Ouro autonomous compute oracle"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.27",
]
